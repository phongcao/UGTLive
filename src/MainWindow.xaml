<Window x:Class="UGTLive.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UGTLive"
        mc:Ignorable="d"
        Title="Transparent Screen Capture" 
        Height="{Binding InitialHeight}" Width="{Binding InitialWidth}"
        WindowStyle="None"
        ResizeMode="CanResizeWithGrip"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        KeyboardNavigation.TabNavigation="None"
        Icon="/media/Icon1.ico">
    <WindowChrome.WindowChrome>
        <WindowChrome ResizeBorderThickness="15" CaptionHeight="10" GlassFrameThickness="0" UseAeroCaptionButtons="False" />
    </WindowChrome.WindowChrome>
      
    
    <Grid Background="Transparent">
        <!-- Overlay is now hosted in a separate OverlayWindow (top-level HWND with WDA_EXCLUDEFROMCAPTURE) -->
        
        <!-- Main UI container - can be hidden via Hide button -->
        <Grid Name="MainBorder" Background="Transparent" Panel.ZIndex="200">
            <!-- Top control bar with auto-sizing grid -->
            <Grid x:Name="TopControlGrid" VerticalAlignment="Top">
                <Grid.RowDefinitions>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- Top resize strip - 10px height (nearly invisible but still hit-testable for dragging) -->
                <Border Grid.Row="0" Background="#01000000" Cursor="SizeNS"
                       WindowChrome.IsHitTestVisibleInChrome="False"/>
                
                <!-- Fixed-height header bar (no wrapping) -->
                <Border Grid.Row="1" x:Name="HeaderBorder" Background="#CC202020" Height="40"
                        Margin="8,0,8,0"
                        MouseLeftButtonDown="Header_MouseLeftButtonDown"
                        WindowChrome.IsHitTestVisibleInChrome="True"
                        ClipToBounds="True">
                    <DockPanel Margin="5,0">
                        <!-- Status display fills remaining space -->
                        <Border x:Name="translationStatusBorder"
                                Background="#DD404040"
                                Padding="8,2,8,2"
                                CornerRadius="3"
                                BorderThickness="1"
                                BorderBrush="#666666"
                                Visibility="Visible"
                                VerticalAlignment="Center"
                                Margin="0,0,5,0"
                                WindowChrome.IsHitTestVisibleInChrome="True">
                            <StackPanel>
                                <TextBlock x:Name="translationStatusLabel"
                                           Foreground="#FFDDDDDD"
                                           FontSize="12"
                                           FontWeight="SemiBold"
                                           Text="Stopped"
                                           VerticalAlignment="Center"
                                           TextAlignment="Center"
                                           TextTrimming="CharacterEllipsis"/>
                                <ProgressBar x:Name="translationProgressBar"
                                             Height="3"
                                             IsIndeterminate="False"
                                             Margin="0,2,0,0"
                                             Visibility="Collapsed"
                                             Foreground="#FF2EA043"
                                             Background="#33FFFFFF"
                                             BorderThickness="0"/>
                            </StackPanel>
                        </Border>
                    </DockPanel>
                </Border>
            </Grid>
            
            <!-- Red capture area border - only around the actual capture area below the header -->
            <Border Margin="8,43,8,8" BorderBrush="#8FFF0000" BorderThickness="7" Background="Transparent" IsHitTestVisible="False"/>
            
            <!-- Resize grip indicators -->
            <Rectangle Width="24" Height="24" Fill="#80808080"
                      HorizontalAlignment="Right" VerticalAlignment="Bottom"
                      Cursor="SizeNWSE"/>
        </Grid>
    </Grid>
</Window>